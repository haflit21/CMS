{% extends('::admin.html.twig') %}

{% block body %}

	<div class="row-fluid">
		<div class="span12">
			{% for flashMessage in app.session.flashbag.get('info') %}
			    <div class="alert alert-info">
			    	<button class="close" data-dismiss="alert" type="button">×</button>
			        <strong>Notice</strong>
			        {{ flashMessage }}
			    </div>
			{% endfor %}
			<h1 class="pull-left">{% trans %}Category{% endtrans %}</h1>

             <a href="{{ path('languages') }}" class="button btn-cancel pull-right"><i class="admin-icon-left-open-1"></i> {% trans %}Back{% endtrans %}</a>
			<div class="clear h30"></div>
            <div class="row">
                <div class="span8">
				{% if language.id != 0 %}
					<form class="form-horizontal" action="{{path('languages_edit', {'id' : language.id })}}" method="post" {{ form_enctype(form) }}>
				{% else %}
					<form class="form-horizontal" action="{{path('languages_new')}}" method="post" {{ form_enctype(form) }}>
				{% endif %}

					{{ form_errors(form) }}

					{{ form_widget(form) }}



					<div class="form-actions">
					    <a href="{{ path('languages') }}" class="button btn-cancel">{% trans %}Cancel{% endtrans %} <i class="admin-icon-cancel"></i></a>
                        <button type="submit" class="button btn-main">{% trans %}Validate{% endtrans %} <i class="admin-icon-ok"></i></button>
				    </div>

				</form>
			</div>
		</div>
	</div>

{% endblock body %}
